version: "3.8"

services:
  web:
    build: .
    ports:
      - "3000"
    env_file:
      - .env.local

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - web